# Base (mismas versiones que ya usás)
fastapi==0.116.1
uvicorn[standard]==0.35.0
psycopg2-binary==2.9.10
sqlalchemy==2.0.42
pydantic==2.11.7
alembic==1.16.4
apscheduler==3.11.1
python-jose[cryptography]==3.5.0
python-multipart==0.0.22
reportlab==4.1.0
openpyxl==3.1.5

# NUEVO: configuracion validada. Reemplaza os.getenv sueltos en database.py
# y security.py.
pydantic-settings>=2.3,<3   # NoDecode requiere >=2.3

# Testing
pytest==8.4.1
pytest-cov==6.0.0
httpx==0.28.1          # requerido por fastapi.testclient

# ----------------------------------------------------------------------
# SACADOS a proposito:
#
#   python-dotenv   -> pydantic-settings ya lee el .env
#   passlib[bcrypt] -> estaba en requirements pero security.py nunca lo
#                      importo: el hashing es pbkdf2 con hashlib puro.
#                      Instalarlo sin usarlo trae vulnerabilidades ajenas.
#
# A considerar mas adelante:
#   argon2-cffi     -> hashing mas fuerte (ver nota al pie de security.py)
#   sentry-sdk      -> con varios clientes vas a querer enterarte de los
#                      errores antes de que te llamen por telefono
#   ruff            -> linter, reemplaza flake8 + isort + black
# ----------------------------------------------------------------------
